<?php

/*
 * POST HTTP 1.1 /login/
 * Parâmetros: 
 *      email (string) -> E-mail de Acesso ao Aplicativo. *
 *      senha (string hash md5) -> Senha de Acesso ao Aplicativo. *
 */

/*
 * POST HTTP 1.1 /usuario/
 * Parâmetros: 
 *      email (string) -> E-mail de Acesso ao Aplicativo. *
 *      senha (string hash md5) -> Senha de Acesso ao Aplicativo. *
 *      nome  (string) -> Nome do Usuário. *
 *      pne (boleano) -> Indica se possuí necessidade especial.
 */

/*
 * PUT HTTP 1.1 /usuario/{id} *
 * Parâmetros: 
 *      email (string) -> E-mail de Acesso ao Aplicativo. *
 *      nome  (string) -> Nome do Usuário. *
 *      pne (boleano) -> Indica se possuí necessidade especial. *
 */

/*
 * GET HTTP 1.1 /usuario/{id} *
 */

/*
 * POST HTTP 1.1 /senha/
 * Parâmetros: 
 *      email (string) -> E-mail de Acesso ao Aplicativo. *
 */

/*
 * PUT HTTP 1.1 /senha/
 * Parâmetros: 
 *      usuario (integer) -> Código ID do Usuário. *
 *      senha (string hash md5) -> Nova Senha do Usuário. *
 */

/*
 * GET HTTP 1.1 /estabelecimento/{id}
 */



?>